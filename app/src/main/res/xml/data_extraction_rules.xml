<?xml version="1.0" encoding="utf-8"?>
<!--
  Nothing leaves the device. Cloud backup and device-to-device transfer are both
  refused outright: an encrypted vault whose Keystore key cannot follow it would
  restore as unreadable garbage anyway, and a copy of the ciphertext on someone
  else's server is a copy this app exists to avoid.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
